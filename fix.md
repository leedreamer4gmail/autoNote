# 你是一个网络专家和vpn专家且精通各种网络编程

# rules
- ❌️不要修改fix.md，这是给你看的任务书而已
- 思考的内容只写在分析.txt里，分析报告把之前的清空，只写新的分析结果
- 工作区的内容直接执行，代码直接改

# 思考（这个区的东西只思考，把思考结果写进 `分析.txt`,但是**不改代码**，不改其他文件只写分析报告）
- [x] enable`分析.txt` 续写（否则重写）
- 要写上如何验证的测试方案



# 工作（这个区的东西马上执行）
这个simplenote_relay目录是不是没有用的？没用整个目录删掉
把这个autoNote push上github，现在github上我创建了同名仓库，把这个项目push上去

# 执行完以后总结经验并更新手册（只写工作区的经验，思考去的不算经验）
- 如果在执行过程中遇新问题并解决，也算是新的经验，也要写进经验里
- 如果有对旧经验的改进和变动，要及时写进经验笔记里，保持经验的更新和准确
- 如果有新的经验，按照下面的规则写到云笔记里，如果没有新的经验，就不用写了
- 如果是关于云笔记simplenote的问题，把最新的经验加在 onlinenote.md 并且同步到simplenote的笔记里，**名字一定要相同不然对不上**
    - 包括成功经验
    - 包括踩的坑和解决的办法
    - 要对以后同类事件有知道作用

- 如果是修复别的问题，单独写一个笔记经验，放到exp目录下，然后同步到云笔记，名字要跟笔记里的一样，命名规则如下：
	1. 针对某个平台的经验，命名为：<平台名称>经验，例如 阿里云经验.md，里面可以有阿里云的各种经验，不用每个经验都命名为阿里云经验，分成不同大区块就行了
	2. 针对某个软件的经验，命名为：<软件名称>经验，例如 openvpn经验.md
	3. 针对某个功能的经验，命名为：<功能名称>经验，例如 ssh使用经验.md
	4. 优先级就按123，命名了前面的就不用后面的了，如果前面没有合适的，就用后面的命名规则
    - 包括问题描述
    - 包括解决办法
    - 包括影响范围
